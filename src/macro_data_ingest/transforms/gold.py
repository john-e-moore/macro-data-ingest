from __future__ import annotations

import pandas as pd


def to_gold_frame(silver_frame: pd.DataFrame) -> pd.DataFrame:
    """Produce analytics-ready Gold frame from Silver data."""
    raise NotImplementedError("Gold transform is not implemented yet.")
